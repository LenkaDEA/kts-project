export type TitleLinksType = {
    link: string,
    title: string
};

const TitleLinks: TitleLinksType[] = [
    { link: "/", title: "Recipes" },
    { link: "/meals-categories", title: "Meals Categories" },
];

export default TitleLinks;